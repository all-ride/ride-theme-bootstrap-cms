{% extends "cms/frontend/index" %}

{% block layout %}
    <div class="grid">
        <div class="grid--bp-med__3">
            {% for widget in regions['column1'] %}
                {{ widget|raw }}
            {% endfor %}
        </div>
        <div class="grid--bp-med__7">
            {% for widget in regions['column2'] %}
                {{ widget|raw }}
            {% endfor %}
        </div>
        <div class="grid--bp-med__2">
            {% for widget in regions['column3'] %}
                {{ widget|raw }}
            {% endfor %}
        </div>
    </div>
{% endblock %}

